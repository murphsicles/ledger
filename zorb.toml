[package]
name = "@accounting/ledger"
version = "0.1.0"
description = "Double-entry accounting state machine — Accounts, Transfers, balance enforcement, two-phase commit"
license = "MIT"
repository = "https://github.com/murphsicles/ledger"
keywords = ["accounting", "ledger", "double-entry"]
categories = ["accounting", "finance"]
